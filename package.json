{
  "name": "nestfy-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "build": "npm run build:backend",
    "build:backend": "npm install --prefix backend --legacy-peer-deps && npm run build --prefix backend",
    "start": "npm run start:backend",
    "start:backend": "npm run start:prod --prefix backend"
  }
}
